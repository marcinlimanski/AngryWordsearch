<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.marcinlimanski.angrywordsearch.StartActivity" >

    <ListView
        android:id="@+id/listView1"
        android:layout_width="400dip"
        android:layout_height="350dip"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true" >
    </ListView>

    <Button
        android:id="@+id/btnTodaysPuzzle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/listView1"
        android:layout_alignParentBottom="true"
        android:onClick="btnTodaysPuzzleClicked"
        android:text="Todays Puzzle" />

    <Button
        android:id="@+id/btnAddPuzzle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/btnTodaysPuzzle"
        android:layout_alignBottom="@+id/btnTodaysPuzzle"
        android:layout_alignRight="@+id/listView1"
        android:onClick="btnAddPuzzleClicked"
        android:text="Add Puzzle" />

</RelativeLayout>
